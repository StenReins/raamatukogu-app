class Author {
    constructor(author_id, name, average_rating, ratings_count, role) {
        this.author_id = author_id;
        this.name = name,
        this.average_rating = average_rating,
        this.ratings_count = ratings_count,
        this.role = role
    }
}